document.getElementById('video__favorite-btn').innerHTML = '<%= escape_javascript(link_to icon('fas', 'star', class: 'video__favorite-on'), video_favorite_path(video, favorite), method: :delete, remote: true) %>'
document.getElementById('video__favorites-count').innerHTML = '<%= video.favorites_count %>'